name: release

on:
  push:
    tags: ["v*"]

# One lossless repository-wide release queue: different tags must not interleave
# irreversible crates.io/npm publication. `queue: max` retains every waiting
# tag; cancel-in-progress stays false because a publish must never be interrupted.
#
# Fork transition: every job fails closed unless the repository owner explicitly
# sets MAPLE_AGENT_MARKET_RELEASES_ENABLED=true. Do not set it until package
# names, repository URLs, asset policy and publication targets are fork-owned.
concurrency:
  group: pixtuoid-release
  queue: max
  cancel-in-progress: false

# Least-privilege (#215): jobs inherit read-only; the `release` job alone
# carries write + attestation scopes (it creates the GH release and attests
# provenance), and `publish`/`npm` carry id-token for OIDC publishing. A
# compromised action in any other job gets no repo-write.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' }}
    name: check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          # Full history for the tag-on-main ancestry check below.
          fetch-depth: 0
          persist-credentials: false
      # rodio/cpal (the default-on `audio` feature) needs ALSA headers to BUILD
      # on Linux (#633) — alsa-sys's build script PANICS when pkg-config can't
      # find them. The CI workflows install this in every Linux job that builds
      # default features; this workflow had none, so `just clippy` below would
      # have failed on the FIRST tag after #633 — in the one gate that only ever
      # runs at tag time.
      - name: Install ALSA headers
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2 # zizmor: ignore[cache-poisoning] PR caches are merge-ref scoped; stable tags are ancestry-gated to main
        with:
          prefix-key: v1-rust
      - uses: taiki-e/install-action@cargo-nextest
      - uses: extractions/setup-just@v4
      - name: Validate tag commit is on main
        # A release tag pushed at an arbitrary (unreviewed) commit must not
        # build + publish (#215). Pre-release tester tags (v*-win.N / -rc.N)
        # are deliberately exempt — they exist to test commits BEFORE merge.
        if: ${{ !contains(github.ref_name, '-') }}
        run: |
          git fetch origin main --quiet
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::Tag ${GITHUB_REF_NAME} points at ${GITHUB_SHA}, which is not an ancestor of origin/main — release tags must be cut from main"
            exit 1
          fi
      - name: Validate tag matches Cargo.toml version
        run: |
          # Pre-release suffixes (-win.1, -rc.1) validate against the base version —
          # the suffix is release-channel metadata, not a Cargo version.
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          TAG_VERSION="${TAG_VERSION%%-*}"
          CARGO_VERSION=$(grep -m1 '^version' Cargo.toml | cut -d'"' -f2)
          if [ "$TAG_VERSION" != "$CARGO_VERSION" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match Cargo.toml version ${CARGO_VERSION}"
            exit 1
          fi
      # Single-sourced through the justfile (same recipes as PR CI + the hooks).
      # Intentionally lighter than `just preflight` (which also runs lint + hack):
      # the tag comes from main, which already passed the full CI matrix — here we
      # re-verify fmt/clippy/tests against the exact tagged commit (`just test`
      # uses nextest).
      - run: just fmt-check
      - run: just clippy
      - run: just test

  build:
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' }}
    name: build (${{ matrix.target }})
    needs: check
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
          - target: x86_64-apple-darwin
            os: macos-14
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            cross: true
          # aarch64 cross-links on the same x64 runner (Tier 1; VC ARM64 components preinstalled).
          # windows-latest now the VS2026 migration has settled; unlike ci-tests.yml's windows-test
          # this build is validated only at TAG time (release.yml doesn't run on PRs), so watch
          # the first tagged run after this and re-pin to a dated label if it regresses.
          - target: x86_64-pc-windows-msvc
            os: windows-latest
          - target: aarch64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2 # zizmor: ignore[cache-poisoning] PR caches are merge-ref scoped; stable tags are ancestry-gated to main
        with:
          prefix-key: v1-rust
          key: ${{ matrix.target }}

      - uses: extractions/setup-just@v4

      - name: Install cross
        if: matrix.cross
        uses: taiki-e/install-action@cross

      - name: Install musl-tools
        if: contains(matrix.target, 'musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build
        # Linux artifacts build WITHOUT the audio feature: musl can't link
        # ALSA statically and the aarch64 cross image has no ALSA headers.
        # Prebuilt Linux = silent; from-source Linux gets audio (#633).
        run: just build-target ${{ matrix.target }} "${{ matrix.cross || 'false' }}" "${{ contains(matrix.target, 'linux') && '--no-default-features' || '' }}"

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          VERSION="${GITHUB_REF_NAME}"
          DIR="pixtuoid-${VERSION}-${{ matrix.target }}"
          mkdir "$DIR"
          cp "target/${{ matrix.target }}/release/pixtuoid" "$DIR/"
          cp "target/${{ matrix.target }}/release/pixtuoid-hook" "$DIR/"
          cp LICENSE "$DIR/"
          tar czf "${DIR}.tar.gz" "$DIR"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # Flat zip — exes at zip root so Expand-Archive doesn't double-wrap.
          # 7z is preinstalled on windows-latest runners.
          VERSION="${GITHUB_REF_NAME}"
          DIR="pixtuoid-${VERSION}-${{ matrix.target }}"
          mkdir "$DIR"
          cp "target/${{ matrix.target }}/release/pixtuoid.exe" "$DIR/"
          cp "target/${{ matrix.target }}/release/pixtuoid-hook.exe" "$DIR/"
          cp LICENSE "$DIR/"
          ( cd "$DIR" && 7z a "../${DIR}.zip" . )

      - uses: actions/upload-artifact@v7
        with:
          name: artifact-${{ matrix.target }}
          path: |
            *.tar.gz
            *.zip
          if-no-files-found: error

  deb:
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' }}
    name: deb (${{ matrix.target }})
    needs: check
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
          - target: aarch64-unknown-linux-gnu
            cross: true
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2 # zizmor: ignore[cache-poisoning] PR caches are merge-ref scoped; stable tags are ancestry-gated to main
        with:
          prefix-key: v1-rust
          key: deb-${{ matrix.target }}

      - uses: extractions/setup-just@v4

      - name: Install cross
        if: matrix.cross
        uses: taiki-e/install-action@cross

      - name: Install cargo-deb
        uses: taiki-e/install-action@cargo-deb

      - name: Build
        # Linux artifacts build WITHOUT the audio feature: musl can't link
        # ALSA statically and the aarch64 cross image has no ALSA headers.
        # Prebuilt Linux = silent; from-source Linux gets audio (#633).
        run: just build-target ${{ matrix.target }} "${{ matrix.cross || 'false' }}" "${{ contains(matrix.target, 'linux') && '--no-default-features' || '' }}"

      - name: Build .deb packages
        run: just deb ${{ matrix.target }}

      - uses: actions/upload-artifact@v7
        with:
          name: deb-${{ matrix.target }}
          path: target/${{ matrix.target }}/debian/*.deb
          if-no-files-found: error

  release:
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' }}
    name: release
    needs: [build, deb]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write # creates the GitHub release
      id-token: write # provenance attestation (OIDC)
      attestations: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum -- *.tar.gz *.deb *.zip > sha256sums.txt

      - name: Generate changelog
        id: changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --latest --strip header

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3 # zizmor: ignore[superfluous-actions] mature release action avoids hand-rolled gh release behavior
        with:
          # Mark v*-rc/-beta/-alpha tags as pre-releases so they don't surface as "latest".
          prerelease: ${{ contains(github.ref_name, '-') }}
          body: ${{ steps.changelog.outputs.content }}
          files: |
            artifacts/*.tar.gz
            artifacts/*.deb
            artifacts/*.zip
            artifacts/sha256sums.txt

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            artifacts/*.tar.gz
            artifacts/*.deb
            artifacts/*.zip

  publish:
    name: publish to crates.io
    # Gate on the FULL build matrix, not just `check`: the crates.io publish is
    # irreversible (a version can never be re-cut), so it must not fire for a tag
    # whose binaries/debs don't build — otherwise crates.io gets 0.12.0 while the
    # GH release (needs: [build, deb]) never ships. Matches the `release` job's gate.
    needs: [check, build, deb]
    # Never publish a pre-release to crates.io — the publish is irreversible;
    # tester pre-release tags (v*-win.N etc.) build artifacts only.
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' && !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      # crates.io Trusted Publishing (#216): cargo has NO native OIDC, so the
      # auth step below exchanges this id-token claim for a short-lived,
      # auto-revoked crates.io token. The standing CARGO_REGISTRY_TOKEN secret
      # is gone — this claim is the only credential.
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      # `cargo publish` BUILDS each crate to verify it, and pixtuoid's default
      # features pull rodio -> cpal -> ALSA headers. Without these the loop
      # publishes pixtuoid-core, pixtuoid-scene and pixtuoid-hook — irreversibly
      # — and only then fails to compile pixtuoid, stranding the release
      # half-published at a version that can never be reused.
      - name: Install ALSA headers
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev
      - uses: Swatinem/rust-cache@v2 # zizmor: ignore[cache-poisoning] PR caches are merge-ref scoped; stable tags are ancestry-gated to main
        with:
          prefix-key: v1-rust
      - name: Authenticate to crates.io (OIDC trusted publishing)
        id: auth
        uses: rust-lang/crates-io-auth-action@v1
      - name: Publish crates (in dependency order)
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          set -euo pipefail
          # Publish in dependency order. Idempotent: swallow "already on crates.io"
          # (safe re-run of a partially-published tag). Retry ONLY the index-
          # propagation race (#111) — a just-published dep not yet visible to the
          # sparse index when the dependent publishes. Every other failure (auth,
          # validation, network) fails the job, never hidden.
          publish_one() {
            local crate="$1" attempt out
            for attempt in 1 2 3; do
              if out=$(cargo publish -p "$crate" 2>&1); then
                printf '%s\n' "$out"; echo "::notice::$crate published"; return 0
              elif grep -qiE 'already (exists|uploaded)|crate version .* is already' <<<"$out"; then
                printf '%s\n' "$out"; echo "::warning::$crate already on crates.io, skipping"; return 0
              elif grep -qiE 'no matching package|not found in registry|failed to select a version' <<<"$out"; then
                printf '%s\n' "$out"; echo "::warning::$crate: dependency not yet on the index — retry $attempt/3 after 20s"; sleep 20
              else
                printf '%s\n' "$out"; echo "::error::$crate publish failed"; return 1
              fi
            done
            printf '%s\n' "$out"; echo "::error::$crate failed after 3 attempts — index-propagation race, or an unsatisfiable dependency requirement"; return 1
          }
          for crate in pixtuoid-core pixtuoid-scene pixtuoid-hook pixtuoid; do
            publish_one "$crate"
          done

  npm:
    name: publish to npm
    # Same full-matrix gate as the crates.io `publish` job: npm consumes only the
    # `build` tarballs, but an npm publish is also irreversible (no unpublish
    # after 72h), so it must not ship a version whose full artifact set (incl.
    # debs) didn't build — one gate for every irreversible publisher.
    needs: [check, build, deb]
    # Never publish a pre-release to npm — like crates.io, an npm publish is
    # effectively irreversible (no unpublish after 72h). Pre-release tags
    # (v*-win.N / -rc) build artifacts only. Consumes the SAME build artifacts
    # as the release job — npm is a third downstream consumer of the one build
    # matrix, not a parallel build.
    if: ${{ vars.MAPLE_AGENT_MARKET_RELEASES_ENABLED == 'true' && !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write # npm provenance (`npm publish --provenance`)
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@v7 # zizmor: ignore[cache-poisoning] this publisher does not enable setup-node's package cache
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      # npm Trusted Publishing (#216): node 22 bundles npm 10.x, but native OIDC
      # publishing needs npm >= 11.5.1. Now LOAD-BEARING — NODE_AUTH_TOKEN is
      # gone, so OIDC is the only auth path; without this upgrade trusted
      # publishing silently won't engage and the publish fails unauthenticated.
      # (registry-url above STAYS: it sets the publish registry AND is what
      # triggers npm's OIDC detection. setup-node writes an .npmrc authToken
      # that's now empty — npm ignores it when a trusted publisher is
      # configured; if OIDC ever fails to engage, drop registry-url so no
      # token line is written at all.)
      # Pinned EXACTLY (not @11): the publish path is irreversible, so what
      # runs must not float with the registry at release time; bump
      # deliberately (and drop this line once node's bundled npm >= 11.5.1).
      - run: npm install -g npm@11.5.1 # zizmor: ignore[adhoc-packages] exact OIDC-capable npm bootstrap for trusted publishing
      - uses: extractions/setup-just@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Extract binaries per target
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          # Mirrors the `build` matrix + npm/generate.mjs TARGETS. Self-guards: a
          # missing artifact fails loud below; the build↔npm full-set equality is
          # pinned by npm/generate.test.mjs (`just npm-check`, run just above).
          for target in aarch64-apple-darwin x86_64-apple-darwin \
                        x86_64-unknown-linux-musl aarch64-unknown-linux-gnu \
                        x86_64-pc-windows-msvc aarch64-pc-windows-msvc; do
            dest="npm/dist/${target}"
            mkdir -p "$dest"
            tgz="artifacts/pixtuoid-${TAG}-${target}.tar.gz"
            zip="artifacts/pixtuoid-${TAG}-${target}.zip"
            if [ -f "$tgz" ]; then
              # tarball wraps everything in pixtuoid-<tag>-<target>/ — unwrap it
              tar xzf "$tgz" -C "$dest" --strip-components=1
            elif [ -f "$zip" ]; then
              # windows zip is flat — exes at the root
              unzip -o "$zip" -d "$dest"
            else
              echo "::error::missing artifact for ${target} (${tgz} / ${zip})"
              exit 1
            fi
          done

      - name: Test the package generator
        run: just npm-check

      - name: Generate npm packages
        run: node npm/generate.mjs --version "${GITHUB_REF_NAME#v}" --artifacts npm/dist

      - name: Publish (platform packages first, launcher last)
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          # Idempotent: swallow "cannot publish over previously published" so a
          # re-run of a partially-published tag is safe (mirrors the crates.io job).
          publish_one() {
            local dir="$1" name out
            name=$(node -p "require('./${dir}/package.json').name")
            if out=$(npm publish "$dir" --access public --provenance 2>&1); then
              printf '%s\n' "$out"; echo "::notice::${name} published"; return 0
            elif grep -qiE 'cannot publish over|previously published' <<<"$out"; then
              # Match ONLY the over-publish phrase — never a bare 403, which npm
              # also emits for auth/scope/2FA failures we must NOT swallow (the
              # crates.io job's "every other failure fails loudly" discipline).
              printf '%s\n' "$out"; echo "::warning::${name}@${VERSION} already on npm, skipping"; return 0
            else
              printf '%s\n' "$out"; echo "::error::${name} publish failed"; return 1
            fi
          }
          # Platform packages first so the launcher's optionalDependencies always
          # resolve to already-live versions; the launcher publishes last.
          # The `./` prefix is load-bearing: `npm publish npm/pixtuoid` parses the
          # path as a GitHub `owner/repo` shorthand (npm/pixtuoid) and tries to
          # `git ls-remote` it; `./npm/pixtuoid` forces folder interpretation.
          for dir in ./npm/@pixtuoid/cli-*; do publish_one "$dir"; done
          publish_one ./npm/pixtuoid
